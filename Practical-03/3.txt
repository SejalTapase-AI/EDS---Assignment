# 3.2.1 Question Summary:
# Write a Python program using NumPy to perform operations on two 3x3 matrices.
# The program should:
# 1. Perform Addition (A + B)
# 2. Perform Subtraction (A - B)
# 3. Perform Element-wise Multiplication (A * B)
# 4. Perform Matrix Multiplication (Dot Product)
# 5. Find the Transpose of Matrix A

import numpy as np

# Input matrices
print("Enter Matrix A:")
matrix_a = np.array([list(map(int, input().split())) for i in range(3)])

print("Enter Matrix B:")
matrix_b = np.array([list(map(int, input().split())) for i in range(3)])

# Addition
a_result = matrix_a + matrix_b
print("Addition (A + B):")
print(a_result)

# Subtraction
s_result = matrix_a - matrix_b
print("Subtraction (A - B):")
print(s_result)

# Element-wise Multiplication
m_result = matrix_a * matrix_b
print("Element-wise Multiplication (A * B):")
print(m_result)

# Matrix multiplication (dot product)
matrix_multiplication = np.dot(matrix_a, matrix_b)
print("A dot B:")
print(matrix_multiplication)

# Transpose
transpose_a = matrix_a.T
print("Transpose of A:")
print(transpose_a)

# 3.2.2 Question Summary:
# Write a Python program using NumPy to perform horizontal and vertical 
# stacking of two 3x3 matrices. 
# - Horizontal Stacking: Side-by-side stacking.
# - Vertical Stacking: One below the other.
# Input: Two 3x3 matrices entered row by row.
# Output: The result of the Horizontal Stack followed by the Vertical Stack.

import numpy as np

# Input for Matrix 1
# print("Enter Matrix 1 (3x3):")
matrix1 = np.array([list(map(int, input().split())) for _ in range(3)])

# Input for Matrix 2
# print("Enter Matrix 2 (3x3):")
matrix2 = np.array([list(map(int, input().split())) for _ in range(3)])

# Horizontal Stacking
h_stack = np.hstack((matrix1, matrix2))
print("Horizontal Stack:")
print(h_stack)

# Vertical Stacking
v_stack = np.vstack((matrix1, matrix2))
print("Vertical Stack:")
print(v_stack)

# 3.2.3 Question Summary: 
# Write a Python program using NumPy to generate a sequence of numbers.
# Input: 
# 1. Start value (inclusive)
# 2. Stop value (exclusive)
# 3. Step value (increment)
# Output: The generated NumPy array sequence.

import numpy as np

# Reading input values
start = int(input())
stop = int(input())
step = int(input())

# Generating the sequence using np.arange
sequence = np.arange(start, stop, step)

# Printing the generated sequence
print(sequence)

# 3.2.4 Question Summary:
# Write a function 'array_operations(A, B)' that converts two lists into 
# NumPy arrays and performs:
# 1. Arithmetic Operations: Element-wise sum, difference, and product.
# 2. Statistical Operations: Mean, median, and standard deviation of array A.
# 3. Bitwise Operations: Bitwise AND, OR, and XOR on arrays A and B.
# Output: Results for each operation in the specified format.

import numpy as np

def array_operations(A, B):
    # Convert A and B to NumPy arrays
    A = np.array(A)
    B = np.array(B)

    # Arithmetic Operations
    sum_result = A + B
    diff_result = A - B
    prod_result = A * B

    # Statistical Operations
    mean_A = np.mean(A)
    median_A = np.median(A)
    std_dev_A = np.std(A)

    # Bitwise Operations
    and_result = A & B
    or_result = A | B
    xor_result = A ^ B

    # Output results with one space between each element
    print("Element-wise Sum:", ' '.join(map(str, sum_result)))
    print("Element-wise Difference:", ' '.join(map(str, diff_result)))
    print("Element-wise Product:", ' '.join(map(str, prod_result)))
    print(f"Mean of A: {mean_A}")
    print(f"Median of A: {median_A}")
    print(f"Standard Deviation of A: {std_dev_A}")
    print("Bitwise AND:", ' '.join(map(str, and_result)))
    print("Bitwise OR:", ' '.join(map(str, or_result)))
    print("Bitwise XOR:", ' '.join(map(str, xor_result)))

# 3.2.5 Question Summary:
# Write a Python program using NumPy to demonstrate the difference between 
# a 'view' and a 'copy' of an array.
# 1. Create a view of the original array (modifying the view affects the original).
# 2. Create a copy of the original array (modifying the copy does not affect the original).
# Output the state of the original array after each modification to verify behavior.

import numpy as np

# Read input and convert to numpy array
original_array = np.array(list(map(int, input().split())))

# 1. Creating a view
view_array = original_array.view()

# Modify the view (changing the first element to 100)
if len(view_array) > 0:
    view_array[0] = 100

print("Original array after modifying view:", original_array)
print("View array:", view_array)

# 2. Creating a copy
copy_array = original_array.copy()

# Modify the copy (changing the first element to 200)
if len(copy_array) > 0:
    copy_array[0] = 200

print("Original array after modifying copy:", original_array)
print("Copy array:", copy_array)

# 3.2.6 Question Summary: 
# Write a Python program using NumPy to perform the following:
# 1. Searching: Find and print the indices where 'search_value' appears in 'array1'.
# 2. Counting: Count and print how many times 'count_value' appears in 'array1'.
# 3. Broadcasting: Add 'broadcast_value' to each element of 'array1' and print the result.
# 4. Sorting: Sort 'array1' in ascending order and print the result.

import numpy as np

# Input array and values
array1 = np.array(list(map(int, input().split())))
search_value = int(input())
count_value = int(input())
broadcast_value = int(input())

# 1. Searching
indices = np.where(array1 == search_value)[0]
print(indices)

# 2. Counting
count = np.count_nonzero(array1 == count_value)
print(count)

# 3. Broadcasting
broadcasted_array = array1 + broadcast_value
print(broadcasted_array)


# 3.2.7 Question Summary:
# Write a Python program using NumPy to analyze student data from a CSV file.
# The program performs multiple operations including:
# 1. Printing all student details and total student count.
# 2. Extracting roll numbers and specific subject marks.
# 3. Finding minimum/maximum marks in specific subjects.
# 4. Calculating total and average marks per student and per subject.
# 5. Identifying specific students based on score criteria (e.g., <40, >=90).
# 6. Sorting marks and finding specific index positions for certain scores.

import numpy as np

# Load the dataset
a = np.loadtxt("Sample.csv", delimiter=',', skiprows=1)

# 1. Print all student details
print("All student Details:\n", a)

# 2. Find total students
print("Total Students:", a.shape[0])

# 3. Print all student roll numbers
print("All Student Roll Nos", a[:, 0])

# 4. Print Subject 1 marks
print("Subject 1 Marks", a[:, 1])

# 5. Find minimum marks in Subject 2
print("Min marks in Subject 2", np.min(a[:, 2]))

# 6. Find maximum marks in Subject 3
print("Max marks in Subject 3", np.max(a[:, 3]))

# 7. Print all subject marks
print("All subject marks:", a[:, 1:])

# 8. Find total marks of students
print("Total Marks", np.sum(a[:, 1:], axis=1))

# 9. Find the average marks of each student
avg = np.mean(a[:, 1:], axis=1)
print(np.round(avg, 1))

# 10. Find average marks of each subject
print("Average Marks of each subject", np.mean(a[:, 1:], axis=0))

# 11. Example of partial average (Subject 1 and Subject 2)
# print("Average Marks of S1 and S2", np.mean(a[:, 1:3], axis=0))

# 4. Sorting
sorted_array = np.sort(array1)
print(sorted_array)

